[project]
name = "data-pipeline-engine"
version = "0.1.0"
description = "A configurable data processing pipeline engine"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
